## 2026-06-12

### 功能修改

根据用户修改清单完成以下改动：

**1. Bubble 图标替换** (`Bubble.vue`)
- 原 "+" 图标替换为勾选图标 (checkmark)
- 圆球尺寸从 56x56 缩小到 48x48
- 窗口 COLLAPSED_SIZE 同步更新 (`mainWindow.ts`)

**2. 右键编辑功能** (`TaskItem.vue`)
- 新增 `contextmenu` 事件处理
- 右键标题进入内联编辑模式
- 支持 Enter 保存、Escape 取消、Blur 自动保存
- 新增编辑输入框样式

**3. 归档功能** (`TaskPanel.vue`)
- 活跃任务与归档任务分离显示
- 归档区域位于列表底部，可折叠/展开
- 显示归档任务数量
- 归档任务按更新时间倒序排列

构建验证通过。
