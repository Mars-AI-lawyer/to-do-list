# DMG settings for TodoList
filename = '/tmp/TodoList-1.0.0-mac.dmg'
volume_name = 'TodoList'
format = 'UDBZ'

# Window settings
window = {
    'position': (200, 120),
    'size': (540, 380),
    'background': None,
}

# Icon settings
icon_size = 80
app_icon = {
    'icon_size': icon_size,
    'icon_position': (270, 160),
}

# Symlinks - disabled due to sandbox restrictions
symlinks = {}
